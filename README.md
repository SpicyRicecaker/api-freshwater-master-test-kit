This project is not in any way endorsed by API. It is simply a fan-made website that helps with keeping track of tasks the customer must perform when doing water tests.

## Running

```shell
trunk serve --open
```